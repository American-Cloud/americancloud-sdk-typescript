// This file was auto-generated by Fern from our API Definition.

/**
 * @example
 *     {
 *         ipId: "123e4567-e89b-12d3-a456-426614174000",
 *         name: "web-lb",
 *         algorithm: "roundrobin",
 *         publicPort: "80",
 *         privatePort: "8080"
 *     }
 */
export interface CreateLoadBalancerRuleDto {
    /** ID of the public IP address */
    ipId: string;
    /** Name of the load balancer rule */
    name: string;
    /** Algorithm used to distribute traffic across the backend VMs. */
    algorithm: CreateLoadBalancerRuleDto.Algorithm;
    /** Public port the load balancer listens on. */
    publicPort: string;
    /** Port on the backend VMs that traffic is forwarded to. */
    privatePort: string;
    /** Protocol the load balancer accepts. */
    protocol?: CreateLoadBalancerRuleDto.Protocol;
    /** Source CIDR allowed to reach the load balancer. */
    sourceCidrList?: string;
    /** Free-form description of the rule. */
    description?: string;
}

export namespace CreateLoadBalancerRuleDto {
    /** Algorithm used to distribute traffic across the backend VMs. */
    export const Algorithm = {
        Roundrobin: "roundrobin",
        Leastconn: "leastconn",
        Source: "source",
    } as const;
    export type Algorithm = (typeof Algorithm)[keyof typeof Algorithm];
    /** Protocol the load balancer accepts. */
    export const Protocol = {
        Tcp: "tcp",
        Udp: "udp",
        TcpProxy: "tcp-proxy",
        Ssl: "ssl",
    } as const;
    export type Protocol = (typeof Protocol)[keyof typeof Protocol];
}
