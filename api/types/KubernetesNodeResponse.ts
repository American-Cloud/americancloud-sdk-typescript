// This file was auto-generated by Fern from our API Definition.

import type * as AmericancloudApi from "../index.js";

export interface KubernetesNodeResponse {
    /** Unique identifier for the node. */
    id: string;
    /** Hostname of the node. */
    name: string;
    /** Role this node plays in the cluster. */
    role: KubernetesNodeResponse.Role;
    /** Current lifecycle state of the node. */
    state: string;
    /** Private IP address assigned to the node. */
    ipAddress?: string | undefined;
    /** Number of virtual CPU cores allocated to the node. */
    cpuCores: number;
    /** Memory allocated to the node, in megabytes. */
    memoryMb: number;
    /** Root disk attached to the node. */
    rootVolume: AmericancloudApi.KubernetesNodeRootVolume;
    /** When the node was provisioned. */
    createdAt: string;
}

export namespace KubernetesNodeResponse {
    /** Role this node plays in the cluster. */
    export const Role = {
        Control: "control",
        Worker: "worker",
        Unknown: "unknown",
    } as const;
    export type Role = (typeof Role)[keyof typeof Role];
}
